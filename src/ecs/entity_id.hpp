// === Entity ID ===
// Defines the type used to represent entity identifiers in the ECS system.

#ifndef ENTITY_ID_HPP
#define ENTITY_ID_HPP

#include <cstddef>

namespace macroengine {
    using EntityID = std::size_t;
}

#endif
